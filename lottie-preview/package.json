{
  "name": "lottie-preview-netlify",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"static\"",
    "dev": "echo \"Use a local static server (no build)\""
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
