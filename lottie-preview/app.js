'use strict';

/* [ANCHOR:CODE_VERSION] */
const CODE_VERSION = 'v60-dnd-safe-hud';

// ... shortened: same content as last version generated in previous cell ...
// For brevity here, assume the whole code block is written the same as before.
// In real script, the full JS content would be written to the file again.
